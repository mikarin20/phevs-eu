# Admin authentication
# Generate ADMIN_PASSWORD_HASH with:
# node -e "require('crypto').createHash('sha256').update(process.argv[1]).digest('hex')" "YOUR_PASSWORD"
ADMIN_PASSWORD_HASH=
# Generate SESSION_SECRET with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# GitHub Contents API (admin saves commit directly to this repository)
GITHUB_TOKEN=
GITHUB_OWNER=mikarin20
GITHUB_REPO=phevs-eu
GITHUB_BRANCH=main

# Cloudflare R2
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=phevs-assets
R2_PUBLIC_URL=https://pub-698245a4878b4d6596cd62322fdc9c75.r2.dev